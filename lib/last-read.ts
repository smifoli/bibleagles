/**
 * Última posição de leitura (livro/capítulo/versão) — cookie de dispositivo
 * que o BottomNav usa pra "Bíblia" levar de volta pro capítulo em vez de
 * sempre reiniciar na lista de livros.
 */
export const LAST_READ_COOKIE = "last_read_path";
